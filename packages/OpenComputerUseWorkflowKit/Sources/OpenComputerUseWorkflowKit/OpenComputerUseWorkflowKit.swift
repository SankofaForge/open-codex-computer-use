/// Public package marker for the future design-inspiration workflow host.
///
/// The target is intentionally inert until the workflow MCP implementation
/// lands. It does not change the `open-computer-use` executable or the
/// existing Computer Use MCP surface.
public enum OpenComputerUseWorkflowKit {
    /// The planned external configuration contract version.
    public static let configurationSchemaVersion = "workflow-config.v1"
}
